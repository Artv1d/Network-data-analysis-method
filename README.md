# Network-data-analysis-method
# Data Processing Pipeline with Chain of Responsibility

Проект для обработки CSV файлов с использованием паттерна "Цепочка ответственности".

## Установка и использование
```bash
pip install -r requirements.txt
python -m src.app data/hh.csv --target "ЗП" --output ./results
